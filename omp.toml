version = 3
final_space = true

[palette]
  blue = '#8CAAEE'
  closer = '#ACB0BE'
  lavender = '#BABBF1'
  os = '#ACB0BE'
  pink = '#F4B8E4'
  danger = '#FF5555'

[[blocks]]
  type = 'prompt'
  alignment = 'left'
  newline = false 

  [[blocks.segments]]
    template = '🐢 '
    foreground = 'p:os'
    type = 'os'
    style = 'plain'

  [[blocks.segments]]
    template = '{{ .Path }} '
    foreground = 'p:pink'
    type = 'path'
    style = 'plain'
    [blocks.segments.properties]
      folder_icon = ''
      home_icon = ' ~'
      style = 'agnoster_short'
      # ADD YOUR SPECIAL DIRECTORY ICONS HERE:
      [blocks.segments.properties.mapped_locations]
        "/workspaces" = "🏠 "
        "~/coyote" = "🐺 "
        "~/beeline" = "🐝_ "
        "~/lyra" = "🪉 "
        "~/falco-web-lite" = "🐦🚚 "
        "~/falco" = "🐦 "

  [[blocks.segments]]
    template = ' '
    foreground = 'p:closer'
    type = 'text'
    style = 'plain'

[[blocks]]
  type = 'rprompt'
  alignment = 'right'

  [[blocks.segments]]
    # This template checks if we are in a git repo. 
    # If yes: shows branch icon + name. If no: shows warning.
    template = "{{ if .UpstreamIcon }}{{ .UpstreamIcon }}{{ .HEAD }}{{ else }}%F{p:danger} no branch%f{{ end }} "
    foreground = 'p:lavender'
    type = 'git'
    style = 'plain'
    [blocks.segments.properties]
      branch_icon = ' '
      branch_max_length = 10
      fetch_status = true
